# create a function to find the largest number in a list.


def large_number(*a):
    b = [0]
    length = len(*a)
    while b < length:
        if b == 0:
            no1 = b[y]
        else:
            if b[y] > no1:
                no1 == (y)
            else:
                pass
        print(b)


y = large_number(2, 3, 4, 6, 7)
